---
ntp:
  hosts:
    ntp-server:
      ansible_host: <IP-адрес>
      ansible_user: <Имя пользователя>
      ansible_ssh_private_key_file: ~/.ssh/id_rsa


app:
  hosts:
    app-server:
      ansible_host: <IP-адрес>
      ansible_user: <Имя пользователя>
      ansible_ssh_private_key_file: ~/.ssh/id_rsa

db:
  hosts:
    db-server:
      ansible_host: <IP-адрес>
      ansible_user: <Имя пользователя>
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
